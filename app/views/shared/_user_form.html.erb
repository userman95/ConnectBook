<%= form_for(@user) do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <%= f.file_field :image, accept: 'image/jpeg,image/gif,image/png', class: "file-profile-button" %>
  <%= f.submit "Upload profile image", class: "upload-prof-image" %>
<% end %>
