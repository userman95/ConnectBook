<% unless @like.errors.any? %>
  $("#post-<%= @post.id %>-attention .like-display")
    .html("<%= j (render('likes/button', resource: @post)) %>");
  $("#post-<%= @post.id %>-likes").html("<%= @post.likes.count %>");
<% end %>
