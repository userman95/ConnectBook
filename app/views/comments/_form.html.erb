<%= form_for(current_user.comments.build, remote: true, html: {id: "post-#{resource.id}-comment-input"} ) do |f| %>
  <%= f.hidden_field :post_id, value: resource.id %>
  <%= f.text_field :content, placeholder: 'Write a comment...', class: "comment-input" %>
<% end %>
