<% if @post.comments.any? %>
  $('#post-<%= @post.id %>-comments').html('<%= escape_javascript(render @comments) %>');
<% end %>

$(document).ready(function(){
  $('#post-<%= @post.id %>-comment-button').on("click",function(){
    $('#post-<%= @post.id %>-comments').toggle();
  });
});
